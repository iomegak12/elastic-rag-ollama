    2  apt-get update
    3  apt-get install -y make build-essential libssl-dev zlib1g-dev libbz2-dev libreadline-dev libsqlite3-dev wget curl llvm libncurses5-dev libncursesw5-dev xz-utils tk-dev libffi-dev liblzma-dev
    4  curl https://pyenv.run | bash
    5  echo -e 'export PYENV_ROOT="$HOME/.pyenv"\nexport PATH="$PYENV_ROOT/bin:$PATH"' >> ~/.bashrc
    6  echo -e 'eval "$(pyenv init --path)"\neval "$(pyenv init -)"' >> ~/.bashrc
    7  source ~/.bashrc
    9  pyenv install 3.11

   10  mkdir practices
   11  cd practices/
   12  pyenv local 3.11
   13  pyenv virtualenv env
   14  pyenv activate env

   15  curl -fsSL https://ollama.com/install.sh | sh
   16  systemctl edit ollama.service
   17  nano /etc/systemd/system/ollama.service
   18  systemctl daemon-reload
   19  systemctl restart ollama

   20  ollama list
   21  ollama --help
   22  ollama pull --help
   23  ollama pull mistral

   24  git clone https://github.com/srikanthmanvi/RAG-InsuranceCompany
   25  cd RAG-InsuranceCompany/
   29  nano .env
   30  cat .env
   32  pip install llama-index  llama-index-embeddings-ollama llama-index-llms-ollama  llama-index-vector-stores-elasticsearch sentence-transformers  python-dotenv
   42  python3 index.py
   43  python3 query.py
   45  nano query_pii.py
   46. python3 query_pii.py
   
